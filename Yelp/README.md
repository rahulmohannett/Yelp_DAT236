# Yelp Prototype — Restaurant Discovery Platform with AI-Powered Recommendations

**Course:** DATA236 — Distributed Systemss  
**Authors:** Rahul Mohan Devi, Mansi Deshmukh  
**Due:** March 24, 2026

---

## Table of Contents

1. [Tech Stack](#tech-stack)
2. [Prerequisites](#prerequisites)
3. [Installation & Setup](#installation--setup)
4. [Running the Application](#running-the-application)
5. [Project Structure](#project-structure)
6. [Test Accounts](#test-accounts)
7. [API Documentation (Swagger)](#api-documentation-swagger)
8. [Testing Guide — Every Requirement](#testing-guide--every-requirement)
9. [API Endpoints Reference](#api-endpoints-reference)
10. [Environment Variables](#environment-variables)
11. [Database Schema](#database-schema)
12. [Troubleshooting](#troubleshooting)

---

## Tech Stack

| Layer            | Technology                                                    |
|------------------|---------------------------------------------------------------|
| Frontend         | React 18, React Router v6, React Bootstrap 5, Axios, Vite    |
| Backend API      | Python 3.9+, FastAPI, Pydantic v2, SQLAlchemy 2.0            |
| Database         | MySQL 8.0 (InnoDB)                                           |
| Authentication   | JWT (python-jose), bcrypt password hashing (pinned 4.0.1)    |
| AI Pipeline      | LangChain 0.3+, LangGraph 0.2+, Google Gemini 2.5 Flash     |
| Web Search       | Tavily API                                                    |
| API Docs         | Swagger UI (auto-generated by FastAPI at `/docs`)             |

---

## Prerequisites

Ensure the following are installed before proceeding:

- **Python 3.9+** — `python3 --version`
- **Node.js 18+** — `node --version`
- **npm 9+** — `npm --version`
- **MySQL 8.0+** — `mysql --version`
- **Git** — `git --version`
- **Google Gemini API Key** — obtain from [Google AI Studio](https://aistudio.google.com/app/apikey)
- **Tavily API Key** — obtain from [Tavily](https://app.tavily.com/)

---

## Installation & Setup

### Step 1: Clone the Repository

```bash
git clone <repository-url>
cd Yelp_Package
```

### Step 2: Database Setup

Start MySQL and run the initialization script:

```bash
mysql -u root -p

# From inside MySQL shell:
source database/init.sql;
exit;
```

This creates the `yelp_db` database with **10 tables** and inserts sample data (2 test users, 6 restaurants, 5 reviews, 1 user preference record).

To verify:

```bash
mysql -u root -p -e "USE yelp_db; SHOW TABLES; SELECT COUNT(*) FROM restaurants;"
```

You should see 10 tables and 6 restaurants.

### Step 3: Backend Setup

```bash
cd backend

# Create and activate virtual environment
python3 -m venv venv
source venv/bin/activate        # macOS/Linux
# venv\Scripts\activate          # Windows

# Install dependencies
pip install -r requirements.txt

# Create environment file
cp .env.example .env
```

Now edit `backend/.env` with your actual credentials:

```
DATABASE_URL=mysql+pymysql://root:YOUR_MYSQL_PASSWORD@localhost:3306/yelp_db
SECRET_KEY=any-random-secret-string-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
GOOGLE_API_KEY=your-actual-google-gemini-api-key
TAVILY_API_KEY=your-actual-tavily-api-key
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
```

### Step 4: Frontend Setup

```bash
cd ../frontend

# Install dependencies
npm install

# Create environment file
cp .env.example .env
```

The default `frontend/.env` should contain:

```
VITE_API_URL=http://localhost:8000
```

---

## Running the Application

You need **two terminal windows** — one for backend, one for frontend.

### Terminal 1: Start Backend

```bash
cd backend
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Verify: http://localhost:8000 → `{"message":"Welcome to Yelp Prototype API","docs":"/docs","version":"1.0.0"}`

### Terminal 2: Start Frontend

```bash
cd frontend
npm run dev
```

Verify: http://localhost:5173 → Home page with "Discover Your Next Favorite Restaurant"

### Quick Verification

| Check                         | URL                              | Expected                          |
|-------------------------------|----------------------------------|-----------------------------------|
| Backend running               | http://localhost:8000            | JSON welcome message              |
| Swagger docs                  | http://localhost:8000/docs       | Interactive API documentation     |
| Health check                  | http://localhost:8000/health     | `{"status":"healthy"}`            |
| Frontend running              | http://localhost:5173            | Home page with AI assistant CTA   |

---

## Project Structure

```
Yelp_Package/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── config.py                 # Settings from .env (pydantic-settings)
│   │   ├── database.py               # SQLAlchemy engine & session
│   │   ├── main.py                   # FastAPI app, CORS, routers, static files
│   │   ├── models/
│   │   │   └── __init__.py           # 10 SQLAlchemy models (User, Restaurant, Review,
│   │   │                             # ReviewPhoto, Favorite, RestaurantClaim,
│   │   │                             # ChatConversation, ChatMessage, etc.)
│   │   ├── schemas/
│   │   │   └── __init__.py           # Pydantic v2 request/response schemas
│   │   ├── routers/
│   │   │   ├── auth.py               # POST /auth/register, /auth/login
│   │   │   ├── users.py              # GET/PUT /users/me, preferences, profile picture
│   │   │   ├── restaurants.py        # CRUD /restaurants, photo upload, view count
│   │   │   ├── reviews.py            # CRUD /reviews, review photo upload
│   │   │   ├── favorites.py          # GET/POST/DELETE /favorites
│   │   │   ├── owner.py              # Dashboard, analytics, sentiment, claim search
│   │   │   ├── ai_assistant.py       # POST /ai-assistant/chat, persistent conversations
│   │   │   └── history.py            # GET /history/reviews, /history/restaurants
│   │   ├── services/
│   │   │   └── auth.py               # JWT, bcrypt, get_current_user, get_current_owner
│   │   └── ai_assistant/
│   │       ├── __init__.py
│   │       └── chatbot.py            # LangGraph pipeline (6 nodes) + Gemini 2.5 Flash
│   ├── requirements.txt
│   ├── .env.example
│   └── uploads/                      # Runtime: profile pics, restaurant & review photos
├── frontend/
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js                # Dev server port 5173, proxy /api → localhost:8000
│   ├── .env.example
│   └── src/
│       ├── main.jsx                  # React entry point (BrowserRouter)
│       ├── App.jsx                   # Routes and auth state management
│       ├── index.css                 # Global styles + responsive breakpoints
│       ├── components/
│       │   ├── Navbar.jsx            # Navigation bar with role-based links
│       │   └── RestaurantCard.jsx    # Reusable clickable restaurant card
│       ├── pages/
│       │   ├── HomePage.jsx          # Landing page with AI assistant CTA
│       │   ├── LoginPage.jsx         # Login form
│       │   ├── SignupPage.jsx        # Registration (customer/owner, city for owners)
│       │   ├── SearchPage.jsx        # Filters: cuisine pills, dietary/ambiance pills,
│       │   │                         # price toggles, sort, zip code, keyword buttons
│       │   ├── RestaurantDetailPage.jsx  # Full detail, photos, hours, reviews CRUD,
│       │   │                             # review photos, favorites, photo upload
│       │   ├── ProfilePage.jsx       # Profile editor + dining preferences + photo upload
│       │   ├── FavoritesPage.jsx     # Grid of favorited restaurants
│       │   ├── HistoryPage.jsx       # Tabs: reviews (with restaurant names) + restaurants
│       │   ├── AddRestaurantPage.jsx # Hours UI, amenities, photo URLs with previews
│       │   ├── ChatPage.jsx          # Dedicated AI chat with conversation sidebar,
│       │   │                         # persistent history, rec cards, quick actions
│       │   └── OwnerDashboard.jsx    # Stats, sentiment, analytics, claim search,
│       │                             # edit modal with hours/amenities
│       └── services/
│           ├── api.js                # Axios instance with JWT interceptor
│           ├── authService.js        # Login, register, logout, getCurrentUser
│           ├── chatbotService.js     # Chat, conversations CRUD
│           ├── restaurantService.js  # Restaurant CRUD, photo upload, zip_code search
│           ├── reviewService.js      # Review CRUD, review photo upload
│           └── userService.js        # Profile, preferences, favorites, photo upload
├── database/
│   └── init.sql                      # Full schema (10 tables) + sample data
└── README.md
```

---

## Test Accounts

The `init.sql` script seeds the following accounts (password for both is `password`):

| Account          | Email              | Password   | Role     |
|------------------|--------------------|------------|----------|
| Test Customer    | customer@test.com  | password   | customer |
| Test Owner       | owner@test.com     | password   | owner    |

The Test Owner already owns Pasta Paradise (id=1) and Sushi Master (id=2).  
Taco Fiesta (3), Green Leaf Cafe (4), The Golden Wok (5), and Bombay Spice (6) are unclaimed.

---

## API Documentation (Swagger)

FastAPI auto-generates interactive Swagger documentation at:

- **Swagger UI:** http://localhost:8000/docs — click "Try it out" on any endpoint
- **ReDoc:** http://localhost:8000/redoc — alternative read-only docs

To authenticate in Swagger:

1. Call `POST /auth/login` with `{"email":"customer@test.com","password":"password"}`
2. Copy the `access_token` from the response
3. Click the **Authorize** button (top right of Swagger UI)
4. Enter: `Bearer <your_token>` and click Authorize
5. All protected endpoints will now work

---

## Testing Guide — Every Requirement

### User (Reviewer) Features

#### 1. Signup
1. Go to http://localhost:5173/signup
2. Fill in: Name, Email, Password, Confirm Password
3. Select "Customer" as account type → Sign Up
4. Redirected to home page, logged in
5. Password stored as bcrypt hash (`$2b$12$...`)

#### 2. Login/Logout
1. http://localhost:5173/login → `customer@test.com` / `password`
2. Login → home page with user name in navbar
3. Logout → token cleared, protected routes redirect to login

#### 3. Profile Page
1. Click name in navbar → Profile
2. All fields present: name, phone, about me, city, state (abbreviated, auto-uppercase, max 2 chars), country (dropdown), languages, gender, profile picture upload (camera icon on avatar)
3. Edit and save → changes persist on refresh

#### 4. User Preferences
1. Profile page → "Dining Preferences" section
2. Fields: cuisine (comma-separated), price range (dropdown), dietary needs, location, ambiance, sort preference (rating/distance/popularity/price)
3. Save → preferences used by AI assistant

#### 5. Restaurant Search
1. Navbar → "Search Restaurants"
2. All 6 sample restaurants load
3. Filters: search box, cuisine pills (clickable), collapsible filter panel with price toggles, zip code, dietary pills, ambiance pills, sort buttons, popular keyword buttons
4. Results update on "Search" or "Apply Filters"
5. Click card → restaurant detail page

#### 6. Restaurant Details
1. Click any restaurant → full detail view
2. Visible: name, cuisine, price, description, address/city/state/zip, phone, website, hours of operation (day-by-day), photo gallery (scrollable), average rating + stars, review count, all reviews with user name/rating/text/date, photo upload button

#### 7. Add Restaurant
1. Navbar → "Add Restaurant"
2. Form: name (required), description, cuisine (dropdown), price range, address, city, state, zip, phone, website, hours of operation (day-by-day with "Apply Monday to all"), amenities (comma-separated), photo URLs with previews
3. Submit → redirected to new restaurant detail page

#### 8. Reviews
1. Restaurant detail → "Write a Review" → rating (1-5 stars) + comment → Submit
2. Review appears with user name, rating, text, server-generated date
3. Own reviews show ✏️ edit and 🗑️ delete buttons
4. 📷 Add Photo button on own reviews for photo attachments
5. Users can only edit/delete their own reviews (403 otherwise)

#### 9. Favourites
1. Restaurant detail → "Add to Favorites" (heart)
2. Button toggles to "❤️ Favorited"
3. Navbar → "Favorites" → restaurant appears
4. Click "Favorited" to remove

#### 10. User History
1. Navbar → "History"
2. Two tabs: Reviews (with restaurant name, rating, text, date — clickable) and Restaurants Added

#### 11. AI Assistant Chatbot
1. Home page → "Ask AI" → dedicated chat page (`/chat`)
2. Conversation sidebar (left) with history, main chat (right)
3. Quick action buttons: "Find dinner tonight", "Best rated Italian", "Romantic for anniversary", "Vegan options", "Surprise me!"
4. AI uses saved preferences, extracts filters (cuisine, price, dietary, occasion, ambiance), queries DB, ranks results, provides conversational response with reasoning
5. Clickable restaurant recommendation cards link to detail pages
6. "+ New Chat" creates new conversation; old ones persist in sidebar
7. 🗑️ deletes conversations; "Thinking..." spinner during processing
8. Tavily web search for trending/live info when needed

### Restaurant Owner Features

#### 1. Owner Signup
- Select "Restaurant Owner" → city field appears (required)

#### 2. Owner Login
- `owner@test.com` / `password`

#### 3–4. Restaurant Profile & Posting
- Owner Dashboard → "+ Add Restaurant" with all fields including hours, amenities, photos
- `owner_id` auto-set for owner role

#### 5. Claim Restaurant
- Dashboard → "🏷️ Claim Restaurant" → search by name or zip
- Results show unclaimed restaurants → "Claim This"
- Auto-approved; appears in owner's list

#### 6. View Reviews (Read-Only)
- Dashboard → "Analytics" on owned restaurant → recent reviews displayed (no delete)

#### 7. Owner Dashboard
- Overview: total restaurants, views, reviews, avg rating
- Sentiment analysis: positive/neutral/negative breakdown
- Recent reviews across all restaurants
- Per-restaurant analytics: rating distribution bar chart, view count, sentiment
- Edit modal: all restaurant fields including hours and amenities

### Backend Requirements ✓
- FastAPI + MySQL, RESTful APIs, JWT auth, Pydantic validation, proper HTTP status codes, error handling

### Frontend Requirements ✓
- All pages (explore, detail, signup/login, profile, add restaurant, review, AI chat, owner dashboard, claim, analytics)
- Bootstrap responsive design, Axios API calls, loading states, error handling, component separation

### AI Assistant Requirements ✓
- POST `/ai-assistant/chat`, loads preferences, LangChain NLU, filter extraction, DB query, ranking, conversational responses, Tavily web search, persistent conversations

### Non-Functional Requirements ✓
- Responsive (Bootstrap + media queries), accessible (semantic HTML, alt text, aria-labels, keyboard nav), scalable (DB indexes, pagination)

---

## API Endpoints Reference

### Authentication
| Method | Endpoint           | Auth | Description              |
|--------|--------------------|------|--------------------------|
| POST   | `/auth/register`   | No   | Register new user        |
| POST   | `/auth/login`      | No   | Login, returns JWT       |

### Users
| Method | Endpoint                     | Auth | Description                |
|--------|------------------------------|------|----------------------------|
| GET    | `/users/me`                  | Yes  | Get current user profile   |
| PUT    | `/users/me`                  | Yes  | Update profile fields      |
| POST   | `/users/me/profile-picture`  | Yes  | Upload profile picture     |
| GET    | `/users/me/preferences`      | Yes  | Get dining preferences     |
| PUT    | `/users/me/preferences`      | Yes  | Create/update preferences  |

### Restaurants
| Method | Endpoint                       | Auth    | Description                    |
|--------|--------------------------------|---------|--------------------------------|
| GET    | `/restaurants`                 | No      | Search with filters            |
| GET    | `/restaurants/{id}`            | No      | Get details + increment views  |
| POST   | `/restaurants`                 | Yes     | Create restaurant              |
| PUT    | `/restaurants/{id}`            | Owner   | Update restaurant              |
| DELETE | `/restaurants/{id}`            | Owner   | Delete restaurant              |
| POST   | `/restaurants/{id}/photos`     | Yes     | Upload restaurant photo        |

**Search params:** `cuisine`, `price_range`, `city`, `zip_code`, `search`, `keywords`, `skip`, `limit`

### Reviews
| Method | Endpoint                                  | Auth | Description              |
|--------|-------------------------------------------|------|--------------------------|
| GET    | `/reviews/restaurants/{id}/reviews`       | No   | List restaurant reviews  |
| POST   | `/reviews/restaurants/{id}/reviews`       | Yes  | Create review            |
| PUT    | `/reviews/{id}`                           | Yes  | Update own review        |
| DELETE | `/reviews/{id}`                           | Yes  | Delete own review        |
| POST   | `/reviews/{id}/photos`                    | Yes  | Upload review photo      |

### Favorites
| Method | Endpoint              | Auth | Description              |
|--------|-----------------------|------|--------------------------|
| GET    | `/favorites`          | Yes  | Get user's favorites     |
| POST   | `/favorites`          | Yes  | Add to favorites         |
| DELETE | `/favorites/{id}`     | Yes  | Remove from favorites    |

### Owner
| Method | Endpoint                                | Auth  | Description                          |
|--------|-----------------------------------------|-------|--------------------------------------|
| GET    | `/owner/restaurants`                    | Owner | List owned restaurants               |
| GET    | `/owner/dashboard`                      | Owner | Aggregated stats + sentiment         |
| GET    | `/owner/restaurants/{id}/analytics`     | Owner | Per-restaurant analytics + sentiment |
| GET    | `/owner/restaurants/{id}/reviews`       | Owner | Reviews for owned restaurant         |
| GET    | `/owner/claim/search?q=`               | Owner | Search unclaimed restaurants         |
| POST   | `/owner/claims`                         | Owner | Claim a restaurant                   |
| GET    | `/owner/claims`                         | Owner | List my claims                       |

### AI Assistant
| Method | Endpoint                                     | Auth | Description                  |
|--------|----------------------------------------------|------|------------------------------|
| POST   | `/ai-assistant/chat`                         | Yes  | Chat with AI assistant       |
| GET    | `/ai-assistant/conversations`                | Yes  | List all conversations       |
| GET    | `/ai-assistant/conversations/{id}`           | Yes  | Get conversation + messages  |
| DELETE | `/ai-assistant/conversations/{id}`           | Yes  | Delete conversation          |

### History
| Method | Endpoint                | Auth | Description                    |
|--------|-------------------------|------|--------------------------------|
| GET    | `/history/reviews`      | Yes  | User's review history          |
| GET    | `/history/restaurants`  | Yes  | User's restaurant submissions  |

### Utility
| Method | Endpoint    | Auth | Description    |
|--------|-------------|------|----------------|
| GET    | `/`         | No   | Welcome message|
| GET    | `/health`   | No   | Health check   |

---

## Environment Variables

### Backend (`backend/.env`)

```env
DATABASE_URL=mysql+pymysql://root:YOUR_PASSWORD@localhost:3306/yelp_db
SECRET_KEY=a-long-random-secret-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
GOOGLE_API_KEY=your-google-gemini-api-key
TAVILY_API_KEY=your-tavily-api-key
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
```

### Frontend (`frontend/.env`)

```env
VITE_API_URL=http://localhost:8000
```

---

## Database Schema

The `yelp_db` database contains **10 tables**:

| Table                | Description                                          |
|----------------------|------------------------------------------------------|
| `users`              | User accounts (customer and owner roles)             |
| `user_preferences`   | Dining preferences for AI recommendations            |
| `restaurants`        | Restaurant listings with hours, amenities, view count|
| `restaurant_photos`  | Photos attached to restaurants                       |
| `reviews`            | User reviews with 1-5 star ratings                   |
| `review_photos`      | Photos attached to reviews                           |
| `favorites`          | User-restaurant favorite bookmarks                   |
| `restaurant_claims`  | Owner claims on existing restaurants                 |
| `chat_conversations` | Persistent AI chat conversations                     |
| `chat_messages`      | Individual messages within conversations             |

---

## Troubleshooting

**"Module not found" when starting backend:**
Ensure virtual environment is activated (`source venv/bin/activate`) and dependencies are installed (`pip install -r requirements.txt`).

**Database connection error:**
- Verify MySQL is running: `mysqladmin ping -u root -p`
- Verify `DATABASE_URL` in `.env` matches your MySQL credentials
- Verify the database exists: `mysql -u root -p -e "SHOW DATABASES;" | grep yelp_db`

**CORS errors in browser console:**
- Ensure backend is running on port 8000
- Ensure `CORS_ORIGINS` in backend `.env` includes `http://localhost:5173`

**AI chatbot returns errors:**
- Verify `GOOGLE_API_KEY` is valid and has Gemini API access enabled (billing must be active on GCP project)
- Verify `TAVILY_API_KEY` is valid
- Check backend terminal for detailed error logs

**Frontend shows blank page:**
- Check browser console for errors
- Ensure `VITE_API_URL` in `frontend/.env` is `http://localhost:8000`
- Ensure backend is running

**"401 Unauthorized" on protected endpoints:**
- Token may have expired (default 30 min). Login again.
- Ensure the Authorization header is `Bearer <token>` (with space after Bearer).

**Login fails with correct password after fresh DB init:**
- The `init.sql` ships a bcrypt hash generated with `bcrypt==4.0.1`. Ensure that version is installed (pinned in `requirements.txt`). If you upgraded bcrypt, regenerate hashes:
  ```bash
  python -c "from passlib.context import CryptContext; print(CryptContext(schemes=['bcrypt'], deprecated='auto').hash('password'))"
  ```
  Then update: `UPDATE yelp_db.users SET password_hash='<new_hash>';`

**Cannot claim restaurant (400 error):**
- Restaurant may already have an owner. Only unclaimed restaurants (`owner_id = NULL`) can be claimed.
- Unclaimed sample restaurants: Taco Fiesta (3), Green Leaf Cafe (4), The Golden Wok (5), Bombay Spice (6).

---

## Contributors

- **Rahul Mohan Devi**
- **Mansi Deshmukh**

## Acknowledgments

- FastAPI — web framework with auto-generated Swagger docs
- LangChain / LangGraph — AI orchestration pipeline
- Google Gemini 2.5 Flash — large language model for NLU and response generation
- Tavily — real-time web search API
- React Bootstrap — responsive UI components
- SQLAlchemy — Python ORM
